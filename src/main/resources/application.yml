server:
  port: 8080

spring:
  application:
    name: tutoring-manage-system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tutoring_manage_system?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: yrf246810

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: ASSIGN_ID
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    root: info
    org.example.zhao: debug

app:
  jwt:
    secret: "CHANGE_ME_TO_A_RANDOM_LONG_SECRET"
    expire-seconds: 7200
    header: Authorization
    prefix: "Bearer "