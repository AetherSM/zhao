# 整体 ER 图

> 本文档基于 `src/main/resources/sql/schema.sql` 设计，覆盖系统账号、教务业务、财务统计和扩展功能模块。
>  
> 渲染方式：Mermaid，适用于 Typora、Obsidian、GitLab 及支持 Mermaid 的 Markdown 预览器。

## 1. 设计说明

- 系统采用“账号体系”和“业务实体”分离的设计。
- `sys_user` 负责登录认证，`student` 与 `teacher` 负责业务资料。
- 学员和教师都通过绑定表与系统账号建立一对一关系。
- 课程、报名、排课、缴费、考勤构成核心教务链路。
- 评价、学习资源、家校沟通属于扩展业务模块。

## 2. 角色分析与 Chen 风格 ER 图

### 2.1 系统角色（来自 `sys_role`）

- 管理员（`ADMIN`）：维护学员、教师、课程、排课、考勤、统计与账号权限。
- 教师（`TEACHER`）：查看授课排课、发布学习资源、接收评价、沟通消息。
- 财务（`FINANCE`）：管理缴费记录、查看营收统计。
- 学员/家长（`STUDENT`）：报名课程、查看资源、提交评价、家校沟通。

### 2.2 角色驱动的 ER 图（菱形关系风格）

> 这张图按你给的样式绘制：左侧“角色实体”，中间“关系（菱形）”，右侧“业务实体”。

```mermaid
flowchart LR
  ADMIN[管理员]
  TEACHER_ROLE[教师]
  FINANCE_ROLE[财务]
  STUDENT_ROLE[学员/家长]

  R1{管理}
  R2{维护}
  R3{编排}
  R4{记录}
  R5{统计}
  R6{发布}
  R7{评价}
  R8{报名}
  R9{沟通}
  R10{绑定}
  R11{授权}

  E_USER[用户 sys_user]
  E_ROLE[角色 sys_role]
  E_STUDENT[学员 student]
  E_TEACHER[教师 teacher]
  E_CATEGORY[课程分类 course_category]
  E_COURSE[课程 course]
  E_ENROLL[报名 student_course]
  E_SCHEDULE[排课 course_schedule]
  E_PAYMENT[缴费 payment]
  E_ATTEND[考勤 student_attendance]
  E_REVIEW[课程评价 course_review]
  E_RESOURCE[学习资源 learning_resource]
  E_CHAT[消息 chat_message]

  ADMIN --> R1 --> E_STUDENT
  ADMIN --> R1 --> E_TEACHER
  ADMIN --> R1 --> E_CATEGORY
  ADMIN --> R1 --> E_COURSE
  ADMIN --> R3 --> E_SCHEDULE
  ADMIN --> R4 --> E_ATTEND
  ADMIN --> R5 --> E_PAYMENT
  ADMIN --> R10 --> E_USER
  ADMIN --> R11 --> E_ROLE

  TEACHER_ROLE --> R2 --> E_SCHEDULE
  TEACHER_ROLE --> R6 --> E_RESOURCE
  TEACHER_ROLE --> R7 --> E_REVIEW
  TEACHER_ROLE --> R9 --> E_CHAT

  FINANCE_ROLE --> R4 --> E_PAYMENT
  FINANCE_ROLE --> R5 --> E_PAYMENT

  STUDENT_ROLE --> R8 --> E_ENROLL
  STUDENT_ROLE --> R7 --> E_REVIEW
  STUDENT_ROLE --> R9 --> E_CHAT
  STUDENT_ROLE --> R2 --> E_RESOURCE
```

## 3. 分模块 ER 图

### 3.1 账号与权限模块

```mermaid
erDiagram
  SYS_USER ||--o{ SYS_USER_ROLE : has
  SYS_ROLE ||--o{ SYS_USER_ROLE : grants
  SYS_USER ||--o| SYS_USER_TEACHER : binds
  TEACHER ||--o| SYS_USER_TEACHER : bound
  SYS_USER ||--o| SYS_USER_STUDENT : binds
  STUDENT ||--o| SYS_USER_STUDENT : bound

  SYS_USER {
    bigint id PK
    varchar username UK
    varchar password
    varchar real_name
    tinyint status
    tinyint deleted
  }
  SYS_ROLE {
    bigint id PK
    varchar role_name UK
    varchar role_code UK
  }
  SYS_USER_ROLE {
    bigint id PK
    bigint user_id FK
    bigint role_id FK
  }
  SYS_USER_TEACHER {
    bigint id PK
    bigint user_id FK
    bigint teacher_id FK
  }
  SYS_USER_STUDENT {
    bigint id PK
    bigint user_id FK
    bigint student_id FK
  }
  STUDENT {
    bigint id PK
    varchar student_name
    varchar grade
    date enroll_time
  }
  TEACHER {
    bigint id PK
    varchar teacher_name
    varchar subject
  }
```

### 3.2 教务业务模块

```mermaid
erDiagram
  COURSE_CATEGORY ||--o{ COURSE : contains
  STUDENT ||--o{ STUDENT_COURSE : enrolls
  COURSE ||--o{ STUDENT_COURSE : chosen
  COURSE ||--o{ COURSE_SCHEDULE : schedules
  TEACHER ||--o{ COURSE_SCHEDULE : teaches

  COURSE_CATEGORY {
    bigint id PK
    varchar category_name UK
    int sort
  }
  COURSE {
    bigint id PK
    bigint category_id FK
    varchar course_name
    decimal price
    int total_hours
  }
  STUDENT {
    bigint id PK
    varchar student_name
    varchar grade
  }
  TEACHER {
    bigint id PK
    varchar teacher_name
    varchar subject
  }
  STUDENT_COURSE {
    bigint id PK
    bigint student_id FK
    bigint course_id FK
    datetime sign_time
  }
  COURSE_SCHEDULE {
    bigint id PK
    bigint course_id FK
    bigint teacher_id FK
    date schedule_date
    varchar class_period
  }
```

### 3.3 财务与考勤模块

```mermaid
erDiagram
  STUDENT ||--o{ PAYMENT : pays
  COURSE ||--o{ PAYMENT : charged
  STUDENT ||--o{ STUDENT_ATTENDANCE : attends
  COURSE_SCHEDULE ||--o{ STUDENT_ATTENDANCE : records

  STUDENT {
    bigint id PK
    varchar student_name
    varchar grade
  }
  COURSE {
    bigint id PK
    varchar course_name
    decimal price
  }
  COURSE_SCHEDULE {
    bigint id PK
    bigint course_id FK
    bigint teacher_id FK
    date schedule_date
  }
  PAYMENT {
    bigint id PK
    bigint student_id FK
    bigint course_id FK
    decimal amount
    datetime pay_time
  }
  STUDENT_ATTENDANCE {
    bigint id PK
    bigint student_id FK
    bigint schedule_id FK
    tinyint attendance_status
    datetime attendance_time
  }
```

### 3.4 扩展功能模块

```mermaid
erDiagram
  STUDENT ||--o{ COURSE_REVIEW : writes
  COURSE ||--o{ COURSE_REVIEW : receives
  TEACHER ||--o{ COURSE_REVIEW : related
  COURSE ||--o{ LEARNING_RESOURCE : has
  TEACHER ||--o{ LEARNING_RESOURCE : publishes
  SYS_USER ||--o{ CHAT_MESSAGE : communicates

  STUDENT {
    bigint id PK
    varchar student_name
  }
  TEACHER {
    bigint id PK
    varchar teacher_name
  }
  COURSE {
    bigint id PK
    varchar course_name
  }
  SYS_USER {
    bigint id PK
    varchar username UK
  }
  COURSE_REVIEW {
    bigint id PK
    bigint student_id FK
    bigint course_id FK
    bigint teacher_id FK
    int rating
  }
  LEARNING_RESOURCE {
    bigint id PK
    varchar title
    varchar type
    bigint course_id FK
    bigint teacher_id FK
  }
  CHAT_MESSAGE {
    bigint id PK
    bigint from_user_id FK
    bigint to_user_id FK
    tinyint is_read
  }
```

## 4. 核心关系说明

- `sys_user` 与 `sys_role` 是多对多关系，通过 `sys_user_role` 实现权限分配。
- `sys_user` 与 `teacher` 通过 `sys_user_teacher` 形成一对一绑定。
- `sys_user` 与 `student` 通过 `sys_user_student` 形成一对一绑定。
- `student` 与 `course` 是多对多关系，通过 `student_course` 表示报名关系。
- `course_schedule` 将课程和教师关联起来，用于表示某天某节次的授课安排。
- `payment` 记录学员针对某门课程的缴费情况，可用于营收统计和欠费统计。
- `student_attendance` 基于排课记录学员出勤情况。
- `course_review` 表示学员对课程和教师的评价。
- `learning_resource` 表示教师面向课程发布的作业或学习资料。
- `chat_message` 表示系统用户之间的消息往来，支持家校沟通和通知场景。

## 5. 论文或答辩建议画法

- 如果要画“总图”，建议保留本文这张完整 ER 图。
- 如果要画“分模块图”，建议拆成 4 张：
- 账号权限模块：`sys_user`、`sys_role`、`sys_user_role`、绑定表。
- 教务模块：`student`、`teacher`、`course_category`、`course`、`student_course`、`course_schedule`。
- 财务考勤模块：`payment`、`student_attendance`。
- 扩展功能模块：`course_review`、`learning_resource`、`chat_message`。
